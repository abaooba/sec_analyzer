# Python
__pycache__/
*.pyc

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/

# Secrets — never commit real .env files.
# (One was committed by mistake and pushed publicly; the leaked keys must be
#  rotated provider-side. See LEARNINGS.md.)
.env
**/.env
!.env.example

# Local database
backend/sec_analyzer.db
*.db

# Cached / downloaded data
backend/data/raw_filings/
backend/data/processed/
backend/data/factor_cache/

# Personal/local notes (never committed)
*.local.md
